#include<bits/stdc++.h>
using namespace std;

int main(){     
    //  everythig is same as set but it does nt stores in any particular order it has better complexity
    // lower_bound and upper_bound function are not work
    unordered_set<int>us;
    

  






   //functionality of insert in vector can be also , that only increases thr efficiency
   //begin(), end(), rbegin(), rend(), size(), empty() and swap() are same as those of above
    return 0;
}